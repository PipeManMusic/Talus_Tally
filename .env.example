# Talus Tally Environment Configuration Example
# Copy this to .env and uncomment/modify as needed

# ============================================================================
# Environment Mode - Controls asset path resolution
# ============================================================================
# TALUS_ENV=development    # Force development paths (repo/data, repo/assets)
# TALUS_ENV=production     # Force production paths (/opt/talus_tally)
# TALUS_ENV=auto           # Auto-detect based on what exists (default)
#
# When set to 'development', all assets (templates, indicators, icons) will
# be read from and written to the repository directory structure.
#
# When set to 'production', paths under /opt/talus_tally are preferred.
#
# When set to 'auto' (or not set), the system will prefer development paths
# for write operations and use whatever exists for read operations.

TALUS_ENV=development

# ============================================================================
# Explicit Path Overrides (optional)
# ============================================================================
# Override specific directories if needed (takes precedence over TALUS_ENV)

# TEMPLATES_DIR=/custom/path/to/templates
# INDICATOR_CATALOG_PATH=/custom/path/to/indicators/catalog.yaml

# ============================================================================
# Backend Configuration
# ============================================================================
# FLASK_ENV=development
# FLASK_DEBUG=1
# LOG_LEVEL=DEBUG
